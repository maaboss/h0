unsigned char obfuscated[] = {0x59, 0x41, 0x46,
                                 0x41, 0x07, 0x42, 0x41, 0x41};
    unsigned char key = 0x1F; // Example XOR key
    unsigned char password[20];

    // Decode the obfuscated values with XOR
    for (int i = 0; i < sizeof(obfuscated); i++) {
        obfuscated[i] ^= key;
    }

    printf("What's the password?\n");
    scanf("%19s", password);

    // Compare with decoded password
    if (0 == strcmp((char*)obfuscated, "sala-hakkeri-321")) {
        printf("Yes! That's the password. FLAG{Tero-d75ee66af0a68663f15}\n");
    } else {
        printf("Sorry, no bonus.\n");
    }
00000000  70 69 69 6c 6f 73 2d 41  6e 0a                    |piilos-An.|
0000000a

#!/bin/bash

# Navigate to the Ghidra installation directory
cd /opt/ghidra || { echo "Failed to navigate to /opt/ghidra"; exit 1; }

# List files in the directory
echo "Listing files in /opt/ghidra:"
ls

# Execute Ghidra
echo "Starting Ghidra..."
./ghidraRun

# If needed, you could add additional commands or checks here

# Exit the script
echo "Script completed."

```
#!/bin/bash

# Navigate to the Ghidra installation directory
cd /opt/ghidra || { echo "Failed to navigate to /opt/ghidra"; exit 1; }

# List files in the directory
echo "Listing files in /opt/ghidra:"
ls

# Execute Ghidra
echo "Starting Ghidra..."
./ghidraRun

# If needed, you could add additional commands or checks here

# Exit the script
echo "Script completed."

```




    
